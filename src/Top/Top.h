#pragma once
#include "cinder/app/AppNative.h"
#include "cinder/gl/gl.h"
enum {
	WINDOW_WIDTH = 1600,
	WINDOW_HEIGHT = 900
};
float const WorldScale = 100.0f;
const ci::Vec2f WINDOWSIZE = ci::Vec2f(WINDOW_WIDTH, WINDOW_HEIGHT);